Optional assets for shipping builds:

  app.ico   — Windows application icon (used by dev/build/CEREBRO.spec when this file exists).

Place app.ico in this folder (dev\packaging\), then run dev\scripts\build_windows.ps1 from the repo root.
