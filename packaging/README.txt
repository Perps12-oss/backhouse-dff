Optional assets for shipping builds:

  app.ico   — Windows application icon (used by CEREBRO.spec when this file exists).

Place app.ico in this folder (packaging\), then run scripts\build_windows.ps1.
