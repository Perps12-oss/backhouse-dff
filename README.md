# CEREBRO

Fast duplicate file finder (Flet desktop UI).

## Run

```bash
pip install -r requirements.txt
python main.py
```

Windows: double-click `Run CEREBRO.bat`  
Linux/macOS: `./run.sh`

## Development

Tests, docs, build scripts, and packaging live under **`dev/`** — see [dev/README.md](dev/README.md).
